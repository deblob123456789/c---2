/*
The way a constructor works:
Vector2 : name
(float _x, float _y) : parameters
: x(_x), y(_y) : inititialization

Initializing can also call its own constructors, if you are calling a variable from another class that needs it
*/
/*
Vector2(float _x, float _y) : x(_x), y(_y)
{
	//do thing
}

Entity::Entity(float _x, float _y) : position(_x, _y)
{
}
*/