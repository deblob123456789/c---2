#include "Mob.h"

/*Mob::Mob
	(float _x, float _y,
	float _maxHP, float _currentHP,
	float _directionx, float _directiony, float _speed) :

	Entity(Vector2(_x, _y)),
	Alive(maxHP, currentHP),
	AMovable(Vector2(_directionx, _directiony), speed(_speed))
{
	setMaxHP(_maxHP);
	setcurrentHP(_currentHP);
	std::cout << "Mob created at x = " << _x << " and y = " << _y << ", with " << _maxHP << " max HP and " << _currentHP << " current HP " << std::endl;
}*/